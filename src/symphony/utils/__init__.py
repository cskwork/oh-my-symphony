"""Symphony utility modules.

Small, self-contained helpers with low coupling to the orchestrator core.
Grouped here so the top-level package surface stays focused on the domain
(``workflow``, ``orchestrator``, ``trackers``, ``backends``, etc.).
"""
