# v0.21.1 release handoff

**Paused:** 2026-08-24 (Asia/Seoul)  
**Objective:** ship the corrective coverage-hardening release only after every
local, remote, review, packaging, and publication gate is green.  
**Current branch:** `test/coverage-100-v0.21.1`  
**Implementation head before this handoff:** `9ebc2e6`  
**Release status:** **HOLD — do not merge, tag, or publish yet.**

## Plain-language state

The implementation is locally green and exact combined coverage is now
100.00%. The current candidate also builds and installs successfully. It has
not been pushed, so GitHub has not run the new native Linux/Windows gates.
Independent review found one important CI issue that must be fixed first: the
browser job can exit green when every browser test skips after Chromium fails
to launch.

No remote branch, merge, version bump, tag, or v0.21.1 GitHub Release has been
created.

## Branch and remote state at pause

- Local feature branch: `test/coverage-100-v0.21.1` at `9ebc2e6` before this
  handoff commit; it has no upstream.
- `origin/dev`: `aaf2b72da3b97fb9d0b71fb881fafea38f59de6d`
- `origin/main`: `260466e293823aa4df3da3eda87cf2ac0c628555`
- Existing public release/tag: `v0.21.0`
- Working tree was clean immediately before adding this handoff.

Implementation commits after v0.21.0:

```text
9ebc2e6 ci(test): enforce combined coverage
00ed614 refactor(core): encode parser invariants
86d9a04 test(coverage): close platform boundaries
f94e172 fix(core): harden boundary handling
dc2e2eb test(coverage): add behavior contracts
3f5f096 docs(test): plan 100% coverage release
```

## Verified local evidence

### Exact coverage

- Combined Windows/Linux result: **22,962 statements, 0 missing, 100.00%**.
- The command enforced both `coverage report --fail-under=100` and a JSON
  assertion that `missing_lines == 0` and `percent_covered == 100`.
- Windows full coverage left only 14 POSIX-native statements; the separate
  read-only Linux container executed all 14 before combination.

Evidence:

```text
C:\Users\a\AppData\Local\Temp\opencode\windows-coverage-final\.coverage.Windows
C:\Users\a\AppData\Local\Temp\opencode\windows-coverage-final\coverage-windows.json
C:\Users\a\AppData\Local\Temp\opencode\windows-coverage-final\junit.xml
C:\Users\a\AppData\Local\Temp\opencode\linux-coverage-final\.coverage.Linux
C:\Users\a\AppData\Local\Temp\opencode\linux-coverage-final\coverage-linux.json
C:\Users\a\AppData\Local\Temp\opencode\combined-coverage-final\coverage-combined.json
```

### Windows and browser E2E

- Native Windows full suite with `SYMPHONY_BROWSER_E2E=1`:
  **3,126 passed, 80 declared skips, 0 failures, 0 errors**.
- Runtime: 691.90 seconds.
- Stderr: empty.
- JUnit browser cases: **5 selected, 5 executed, 0 skipped**.

### Linux platform proof

- Disposable `python:3.12-slim` container, checkout mounted read-only.
- Result: **30 passed, 1 expected Windows-only skip**.
- Covered native `fcntl` locking/unlocking, POSIX skill symlink, executable
  launcher modes, preview process-session creation, and process-group cleanup.
- Docker was used only as a separate Linux test environment, never as a
  Windows runtime dependency. A Docker Desktop stop was requested after the
  proof completed.

### Focused and static gates

- Behavior-focused coverage cases: **807 passed**.
- Ruff: passed for `src`, `tests`, and `scripts`.
- Pyright: `0 errors, 0 warnings`.
- Translation validation: `551 keys`, English/Korean complete and used.
- Workflow YAML parsed successfully.
- `git diff --check`: clean.
- Local `actionlint` was unavailable; GitHub must validate the workflow.

### Packaging smoke (mechanics only; still version 0.21.0)

- Built wheel and sdist successfully.
- Twine check passed for both.
- Fresh Python 3.12 environment installed the wheel and verified:
  package metadata, `symphony.__version__`, bundled web static assets, and
  `symphony --help`.

Artifacts:

```text
C:\Users\a\AppData\Local\Temp\opencode\package-prep-0.21.0\oh_my_symphony-0.21.0-py3-none-any.whl
C:\Users\a\AppData\Local\Temp\opencode\package-prep-0.21.0\oh_my_symphony-0.21.0.tar.gz
C:\Users\a\AppData\Local\Temp\opencode\package-smoke-0.21.0-20260824072812
```

These prove packaging mechanics only. **Do not publish them**; final artifacts
must be rebuilt after the v0.21.1 metadata commit.

### Doctor diagnosis

`symphony doctor ./WORKFLOW.md` appeared silent only inside the managed Codex
filesystem sandbox. `check_workspace_root` performs a real write probe in
`C:\Users\a\symphony_workspaces`, outside that sandbox's writable root.

- Sandboxed `NamedTemporaryFile` stalled.
- Unsandboxed equivalent: 0.00046 seconds and deleted cleanly.
- Unsandboxed full doctor: 2.8 seconds.
- The command exits 1 because the source repository is intentionally protected
  from running workers; all remaining doctor rows passed.

No doctor code was changed. Run this release diagnostic outside the managed
filesystem sandbox.

## Independent review results

### Production code review

- Critical: 0
- Important: 0
- Minor: coverage XML is required by the written plan but CI currently emits
  only JSON.
- Verdict: implementation is ready to merge after release gates, but not ready
  to publish.

The reviewer found no threshold gaming, new coverage omissions, disguised
breaking change, or blanket meaningless assertion in the reviewed range.

### CI/release adversarial review

- Critical: 0
- **Important blocker:** no GitHub run exists for exact head `9ebc2e6`; the
  branch has not been pushed.
- **Important blocker:** all five browser E2E tests convert Chromium launch
  failure into `pytest.skip`. The Windows browser job can therefore be green
  while executing zero browser tests.
- Expected release work remains: v0.21.1 metadata/changelog, final packaging,
  integration through `dev` and `main`, exact-main CI/Pages, tag, and release.
- Minor: add coverage XML or reconcile the plan; ignore `.coverage.*`; broad
  star-import support modules reduce test symbol provenance.

Overall review verdict: **HOLD** until the browser job fails closed and remote
CI is green.

## Required next actions

Follow this order. Do not skip gates.

1. **Make browser E2E fail closed.**
   - Keep the five existing browser tests.
   - Recommended minimal CI change: emit browser JUnit XML, then assert
     `tests == 5`, `failures == 0`, `errors == 0`, and `skipped == 0`.
   - Alternatively, add an explicit CI-required environment mode that turns
     Chromium launch failure into a test failure rather than a skip.
   - Add/adjust a regression that proves an all-skipped browser run cannot
     satisfy the release job.

2. **Resolve minor CI hygiene.**
   - Add `python -m coverage xml -o coverage.xml`, or explicitly amend the
     plan if XML has no consumer.
   - Add `.coverage.*` to `.gitignore` for safe local reproduction.
   - Re-run YAML parsing, focused tests, Ruff, Pyright, i18n, and diff check.

3. **Commit the CI correction.**
   - Use a Conventional Commit with `Refs: COV-100`.
   - Re-run the independent review on the new head.

4. **Push only the verification branch first.**

   ```powershell
   git push -u origin test/coverage-100-v0.21.1
   gh run list --workflow Tests --branch test/coverage-100-v0.21.1
   ```

   Require green results for static quality, Linux pytest, Windows pytest,
   combined 100% coverage, and Windows browser E2E with zero skips.

5. **Integrate through `dev`.**
   - Fetch and verify remote heads.
   - Merge the verified feature branch into `dev` with a non-fast-forward merge.
   - Push `dev` without force.
   - Wait for the exact `dev` Tests workflow to pass.

6. **Integrate `dev` into `main`.**
   - Use a non-fast-forward merge; do not rewrite history.
   - Do not tag yet.

7. **Prepare v0.21.1 metadata on `main`.**
   - Add the dated v0.21.1 changelog section and comparison links.
   - Synchronize `pyproject.toml`, `src/symphony/__init__.py`, `uv.lock`, and
     `docs/index.html` to `0.21.1`.
   - Commit as a release-preparation commit following repository precedent.

8. **Run final v0.21.1 gates from the exact release commit.**
   - Full native Windows suite with browser E2E and zero browser skips.
   - Full native Linux CI suite.
   - Combined exact coverage: 22,962 statements or the new measured total,
     zero missing, 100.00%.
   - Ruff, Pyright, translations, workflow validation, and diff check.
   - Unsandboxed doctor with the expected protected-source-repository result
     explicitly classified.
   - Build v0.21.1 wheel/sdist, Twine check, fresh Python 3.12 install/import,
     static asset, CLI, and version smoke.

9. **Push `main` and wait for exact-main CI/Pages.**

10. **Publish only after every result above is green.**
    - Create an annotated `v0.21.1` tag on the exact verified main commit.
    - Push the tag without force.
    - Create the GitHub Release with concise corrective release notes.
    - Verify the public tag and release resolve to the same commit.

## Hard safety constraints

- Do not force-push, rewrite, or delete remote history.
- Do not reuse the 0.21.0 package-prep artifacts as v0.21.1.
- Do not lower the coverage threshold, omit sources, or add broad exclusions.
- Do not accept a green browser job with skipped browser tests.
- Do not release directly from the feature branch or from `dev`.
- Do not claim doctor passed in the source repository; its protected-repo FAIL
  is intentional and separate from the diagnosed sandbox hang.
