# Delivery Proof - Frontier 004

## eval_intent

Build the smallest maintainable code authority that enforces the complete AIDT delivery sequence with durable,
freshness-bound evidence and explicit issue-plan approval. Keep prompts non-authoritative, generic auto-merge off,
and all real QA/Git/Jenkins/Jira/dev side effects outside Frontier 004.

## before_state

- On 2026-07-22 the frozen primary command exited 4 because `tests/test_aidt_stage_gates.py` did not exist:
  `pytest -q tests/test_aidt_stage_gates.py tests/test_workflow.py`.
- The AIDT example exposes only `Todo`, `Coordinating`, and `Ready` and explicitly says delivery fields are absent.
- Generic transitions are observed after an editable card change, generic contract checks parse Markdown sections,
  and generic rewind counts are in process memory.

## after_target

- The exact adjacent graph passes and every skip, undeclared rewind, stale/ambiguous/mismatched fact, reused approval,
  fourth low-confidence attempt, projection drift, and same-key promotion race fails closed with a durable reason.
- Restart reconstructs identical stage, attempts, approval/evidence freshness, fence owner, and blocking reason.
- Board/detail/history are sanitized projections of the same controller snapshot.

## command_manifest

- `frozen_repo`: `pytest -q tests/test_aidt_stage_gates.py tests/test_workflow.py`
- `frozen_repo`: focused existing example/orchestrator/tracker/web tests selected by each slice
- `frozen_repo`: full `pytest -q`
- `frozen_repo`: repository Ruff and Pyright commands from project configuration
- `evaluator_owned`: restart/crash/concurrency matrix in a temporary workflow directory
- `evaluator_owned`: fixed-base whitespace/tree and default-off side-effect sentinel checks

## decision_gates

- `auto-fix`: mechanical schema, projection, test, or documentation defect within the frozen contract.
- `no-op`: generic non-AIDT workflow behavior stays on its existing contract and transition paths.
- `ask-user`: any proposal to execute live QA/Git/Jenkins/Jira/dev actions or weaken exact per-issue plan approval.
- `no-op`: production issue-plan approval and future evidence producers remain deny-all until their owner tickets
  install worker-inaccessible/reviewed adapters; fixture authority does not claim live human identity.

## after_evidence

Partial only; Frontier 004 is not complete.

- 004a narrow contract: 30 passed.
- 004a affected worktree/workflow/example matrix: 156 passed.
- Ruff focused: pass.
- Pyright focused: 0 errors, 0 warnings, 0 informations.
- Evidence: `004a-contract-report.md`.

## residual_risk

Frontier 004 can prove record binding and authorization behavior but cannot prove the truth of future external
adapters until Frontiers 005-008 exercise their real commands and environments.
The human-approval adapter remains intentionally unavailable until Frontier 009 proves a worker-inaccessible
operator surface; enabled fixture flows do not authorize production activation.
The durable journal, transition reducer, promotion fence, projection/Core barriers, integrated fake E2E, Jenkins,
and final dev E2E have not been implemented or verified.
